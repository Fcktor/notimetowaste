export default function DatabasePage() {
  return (
    <div className="p-2">
      <p className="text-xs font-semibold uppercase tracking-[0.2em] mb-3" style={{ color: "var(--muted-foreground)" }}>
        // Base de datos
      </p>
      <h1 className="font-display text-2xl font-bold mb-6" style={{ color: "var(--foreground)" }}>
        Base de datos
      </h1>
      <div
        className="rounded-xl p-8 text-center"
        style={{
          background: "var(--muted)",
          border: "1px solid var(--border)",
        }}
      >
        <p className="text-sm" style={{ color: "var(--muted-foreground)" }}>
          Sección en construcción.
        </p>
      </div>
    </div>
  );
}
